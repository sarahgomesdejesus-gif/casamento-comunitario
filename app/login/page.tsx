"use client";
import { FormEvent, useState } from "react";
import { LockKeyhole } from "lucide-react";
export default function LoginPage(){const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const f=new FormData(e.currentTarget);const r=await fetch("/api/admin/login",{method:"POST",body:JSON.stringify({login:f.get("login"),password:f.get("password")})});if(r.ok)location.href="/admin";else setError((await r.json()).message||"Não foi possível acessar.");setLoading(false)}return <main className="login-page"><section className="login-card"><a className="brand login-brand" href="/"><span className="brand-mark">CJ</span><span>Carvalhos de Justiça</span></a><LockKeyhole size={28}/><span className="section-tag">acesso restrito</span><h1>Área administrativa</h1><p className="muted">Entre com seu login e senha para analisar os cadastros.</p><form onSubmit={submit}><label>Login<input required name="login" autoComplete="username"/></label><label>Senha<input required name="password" type="password" autoComplete="current-password"/></label>{error&&<p className="field-error">{error}</p>}<button className="primary" disabled={loading}>{loading?"Entrando...":"Entrar"}</button></form></section></main>}
