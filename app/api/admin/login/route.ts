import { NextResponse } from "next/server";
export async function POST(request: Request){const {login,password}=await request.json();const configured=process.env.ADMIN_LOGIN&&process.env.ADMIN_PASSWORD;if(!configured)return NextResponse.json({message:"O acesso administrativo ainda não foi configurado."},{status:503});if(login!==process.env.ADMIN_LOGIN||password!==process.env.ADMIN_PASSWORD)return NextResponse.json({message:"Login ou senha inválidos."},{status:401});const r=NextResponse.json({ok:true});r.cookies.set("cc_admin","authenticated",{httpOnly:true,secure:true,sameSite:"lax",path:"/",maxAge:60*60*8});return r;}
